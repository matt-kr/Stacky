# Vercel Environment Variables Configuration

# Debug System Configuration
VITE_DEBUG_OVERLAY=1
DEBUG_PASSWORD=your_secure_debug_password_here

# Instructions:
# 1. Set VITE_DEBUG_OVERLAY=1 to enable the debug overlay in production
# 2. Set DEBUG_PASSWORD to a secure password for debug access
# 3. Users can access debug mode by clicking the invisible button in the top-right corner
# 4. Debug mode persists until manually disabled or browser storage is cleared
